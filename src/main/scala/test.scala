//import org.mindrot.jbcrypt.BCrypt
import java.io._

/**
 * Created by sshilpika on 3/6/15.
 */
object test {
  /*val homeDir = System.getProperty("user.home")
  val accessToken = scala.io.Source.fromFile(homeDir + "/githubAccessToken").getLines.next()
  val hashFile = scala.io.Source.fromFile(homeDir + "/bcryptHash")
  val hashed = BCrypt.hashpw(accessToken.trim, BCrypt.gensalt())
  println(hashed+" HASHED")
  println(BCrypt.checkpw(accessToken, hashed))
  val writer = new PrintWriter(new File(homeDir + "/bcryptHash" ))

  writer.write(hashed)
  writer.close()*/

}
